"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const schematics_1 = require("@angular-devkit/schematics");
const core_1 = require("@angular-devkit/core");
const strings_1 = require("@angular-devkit/core/src/utils/strings");
// You don't have to export the function as default. You can also have more than one rule factory
// per file.
function pvp(_options) {
    return (tree, _context) => {
        const sourceTemplates = schematics_1.url('./files');
        const sourceParametrizedTemplates = schematics_1.apply(sourceTemplates, [schematics_1.template(Object.assign(Object.assign(Object.assign({}, _options), core_1.strings), { classIt,
                kebabIt }))
        ]);
        console.log(tree);
        function classIt(value) {
            return strings_1.classify(value);
        }
        function kebabIt(value) {
            return strings_1.dasherize(value);
        }
        return schematics_1.mergeWith(sourceParametrizedTemplates);
        // const { name } = _options;
        // tree.create(`${name}.js`, 'hi');
        // return tree;
    };
}
exports.pvp = pvp;
//# sourceMappingURL=index.js.map